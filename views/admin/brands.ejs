<%- include("../../views/partials/admin/header") %>

<style>
  button {
    background-color: #28a745; /* Green for success */
    color: white; /* Text color */
    border: none;
    padding: 10px 20px; /* Adjust padding to control the button's height and width */
    font-size: 16px; /* Adjust font size */
    width: auto; /* Let the width adjust to content or set a specific width */
    cursor: pointer;

}

button.btn-danger {
    background-color: #dc3545; /* Red for danger */
}

button.btn-success {
    background-color: #28a745; /* Green for success */
}

button a {
    color: white;
}

.center-container {
  margin-left: 367px;
  padding-right: 20px; /* Correct padding */
  margin-top: 90px;
}

  .pagination-container {
    text-align: center;
  }
  .pagination {
    display: inline-block;
  }

  .pagination a,
  .pagination .current-page {
    display: inline-block;
    padding: 5px 10px;
    margin: 0 2px;
    border: 1px solid #ddd;
    text-decoration: none;
    color: #333;
  }

  .pagination a:hover {
    background-color: #f5f5f5;
  }
  .error-message {
    color: red;
  }
  .form-label {
    font-size: large;
  }
  .form-control {
    width: 300px; /* Adjust the width if needed */
    height: 50px; /* Increase the height */
    font-size: 18px; /* Optional: Adjust font size for better appearance */
}
form {
    width: 300px; /* Adjust width as needed */
}

.sub-table {
    position: absolute;
    margin-top: -212px;
    margin-left: 483px;
    font-size: 21px;
   
}
.sub-img {
  position: absolute;
    margin-top: -151px;
    margin-left: 483px;
    font-size: 21px;
}
th ,tr ,td {
  padding: 15px;
}
</style>
<section class="content-main">
  <div class="content-header">
    <div>
      <h2 class="content-title card-title">Brands</h2>
    </div>
  </div>
<div class="center-container">
  <div class="card">
    <div class="card-body">
      <div class="row">
        <div class="col-md-3">
          <form method="post" action="/admin/addBrand" enctype="multipart/form-data">
            <div class="mb-4">
              <label for="product_name" class="form-label">Brand Name</label> <br>
              <input
                type="text"
                name="name"
                placeholder="Type here"
                class="form-control"
              />
            </div><br>
            <label for="product_name" class="form-label">Brand Image</label>
            <div class="input-upload">
              <img src="" alt=""/>
              <input class="form-control border" name="image" type="file" />
            </div>
            <div class="d-grid">
              <button class="btn btn-success mt-20"  type="submit">
                Add Brand
              </button>
            </div>
          </form>
        </div><br>
        <div class="col-md-7 ml-105">
          <div class="table-responsive">
            <table class="table table-hover">
              <thead class="sub-table">
                <tr >
                  <th>Brand</th>
                  <th>Logo</th>
                  <th>Status</th>
                  <th>Block/Unblock/Delete</th>
                </tr>
              </thead>
              <tbody class="sub-img">
                <% data.reverse().forEach((brand, index) => { %>
                  <tr>
                    <td class="text-start"><%= brand.brandName || 'No Brand Name' %></td>
              
                    <td class="text-start">
                      <div
                        class="d-flex align-items-center justify-content-center"
                        style="
                          width: 40px;
                          height: 40px;
                          border-radius: 50%;
                          overflow: hidden;
                        "
                      >
                        <img
                          src="/upload/re-image/<%= brand.brandImage[0] %>"
                          alt="<%= brand.brandName %>"
                          class="img-fluid rounded-circle"
                          style="width: 100%; height: auto"
                        />
                      </div>
                    </td>
              
                    <td class="text-start">
                      <div class="d-flex justify-content-start align-items-center">
                        <% if (brand.isBlocked === true) { %>
                          <button class="btn btn-success btn-sm" style="width: 80px; border-radius: 20px; font-size: 12px;">
                            Unblock
                          </button>
                        <% } else { %>
                          <button class="btn btn-danger btn-sm" style="width: 80px; border-radius: 20px; font-size: 12px;">
                            Block
                          </button>
                        <% } %>
                      </div>
                    </td>
                    
                    
              
                    <td class="text-start">
                      <% if (brand.isBlocked === false) { %>
                        <button class="btn btn-danger" style="width: 90px">
                          <a href="/admin/blockBrand?id=<%= brand._id %>" class="text-white">Block</a>
                        </button>
                      <% } else { %>
                        <button class="btn btn-success" style="width: 90px">
                          <a href="/admin/unBlockBrand?id=<%= brand._id %>" class="text-white">Unblock</a>
                        </button>
                      <% } %>
              
                      <button class="btn btn-danger text-white" style="width: 90px" onclick="confirmDelete('<%= brand._id %>')">
                        Delete
                      </button>
                    </td>
                  </tr>
                <% }) %>
              </tbody>
              
                    
            </table>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
  <div class="pagination-container">
</div>
</section>
<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
<script>
  function confirmDelete(brandId) {
    Swal.fire({
      title: 'Are you sure?',
      text: "Do you want to delete this brand?",
      icon: 'warning',
      showCancelButton: true,
      confirmButtonColor: '#d33',
      cancelButtonColor: '#3085d6',
      confirmButtonText: 'Yes, delete it!'
    }).then((result) => {
      if (result.isConfirmed) {
        window.location.href = `/admin/deleteBrand?id=${brandId}`;
      }
    });
  }
</script>

<%- include("../../views/partials/admin/footer") %>

